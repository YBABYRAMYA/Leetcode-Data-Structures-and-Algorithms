class Solution:
    def search(self, nums: List[int], target: int) -> bool:
        sorted(nums)
        nums[::4].extend(nums[4::])
        if target in nums:
            return True
        else:
            return False
                
        
